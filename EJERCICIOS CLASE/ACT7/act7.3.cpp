ALGORITMO:obtener el segundo elemente de una pila de arriba hacia abajo; dejandola pila sin sus elementos de arriba


pila=initialize;

if (!empty(pila))
	break
else 
	pop(pila)
	if(el puntero del siguiente no apunta a NULL)
		i=pop(pila)
		mostrar i
	else
		break;
